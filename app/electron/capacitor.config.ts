import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.axyo.claudesessions',
  appName: 'Claude Sessions',
  webDir: 'dist'
};

export default config;
